# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Jupyter Notebook
.ipynb_checkpoints

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Virtual environments
venv/
env/
.venv/

# Ignore large model weights (Optional, usually we ignore them but since they are in the directory and you want to push, we can just let them be pushed if small enough, but github limits files > 100MB. The models here are ~33MB and ~6MB so they can be pushed).
*.pth
*.pt
